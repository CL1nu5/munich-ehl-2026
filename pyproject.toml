[project]
name = "munich-ehl-2026"
version = "0.1.0"
description = "Offline model routing and off-policy evaluation for the Viktor Challenge"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sentence-transformers>=6.0.0",
    "matplotlib>=3.11.1",
]
